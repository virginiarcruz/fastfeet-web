import styled from 'styled-components';
import { grayLight, white } from '../../styles/colors';

export const InputContainer = styled.input`
  max-height: 36px;
  background-color: ${white};
  border: 1px solid ${grayLight};
  padding: 4px 6px;
  font-size: 14px;
`;
